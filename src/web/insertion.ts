export type Insertion = {
  // Bottom insertion
  bi?: { [key: string]: string };
  // Insertion bottom end
  be?: { [key: string]: string };
  // Insertion bottom start
  bs?: { [key: string]: string };
  // Insertion top end
  te?: { [key: string]: string };
  // Insertion top start
  ts?: { [key: string]: string };
  // Middle insertion
  mi?: { [key: string]: string };
  // Top insertion
  ti?: { [key: string]: string };
};
